import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-6-testing',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
